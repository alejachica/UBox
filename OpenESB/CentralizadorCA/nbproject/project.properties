build.classes.dir=${build.dir}/jar
build.dir=build
build.generated.dir=${build.dir}/generated
com.sun.jbi.routing=true
com.sun.jbi.routing.bc.autoconnect=true
com.sun.jbi.sa.internal.routing=true
dist.dir=dist
dist.jar=${dist.dir}/CentralizadorCA.zip
jbi.content.additional=${reference.CrearUsuarioOp.dist_se};${reference.ConsultaOperadorUsuario.dist_se};${reference.ConsultaUsuario.dist_se}
jbi.content.component=sun-bpel-engine;sun-bpel-engine;sun-bpel-engine
jbi.service-assembly.description=Represents the Service Assembly of CentralizadorCA
jbi.service-assembly.id=CentralizadorCA
jbi.service-unit.description=Represents this Service Unit
meta.inf=${source.root}/conf
project.ConsultaOperadorUsuario=../ConsultaOperadorUsuario
project.ConsultaUsuario=../ConsultaUsuario
project.CrearUsuarioOp=../CrearUsuarioOp
reference.ConsultaOperadorUsuario.dist_se=${project.ConsultaOperadorUsuario}/build/SEDeployment.jar
reference.ConsultaUsuario.dist_se=${project.ConsultaUsuario}/build/SEDeployment.jar
reference.CrearUsuarioOp.dist_se=${project.CrearUsuarioOp}/build/SEDeployment.jar
resource.ConsultaOperadorUsuario=${project.ConsultaOperadorUsuario}/setup
resource.ConsultaUsuario=${project.ConsultaUsuario}/setup
resource.CrearUsuario=${project.CrearUsuario}/setup
resource.CrearUsuarioOp=${project.CrearUsuarioOp}/setup
source.encoding=UTF-8
source.root=src
src.build.dir=${source.root}/jbiServiceUnits
src.dir=${source.root}/jbiasa
test.dir=test
test.results.dir=${basedir}/test/results
